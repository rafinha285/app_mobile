import { Episode } from "../types/episodeType.ts";
// TODO fazer com q o handle next ep troque o upl do video e o ep para outro sem usar o navigation
const handleNextEp = (eps:Map<number,Episode>,epindex:number,)=>{
    let next_ep = eps.get(epindex+1)
};
// TODO fazer com q o handle previous ep troque o upl do video e o ep para outro sem usar o navigation
const handlePreviousEp = () =>{

};
